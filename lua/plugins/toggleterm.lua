local plugins = {
    {
        "akinsho/toggleterm.nvim",
        version = "*",
        opts = {
            size = 20,
            open_mapping = [[<a-i>]],
            direction = "float",
        }
    },
}

return plugins
